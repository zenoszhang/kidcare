import 'package:flutter/material.dart';

class Radii {
  static const BorderRadiusGeometry k8pxRadius =
      BorderRadius.all(Radius.circular(8));
  static const BorderRadiusGeometry k100pxRadius =
      BorderRadius.all(Radius.circular(100));
}
